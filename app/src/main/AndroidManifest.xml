<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission
        android:name="android.permission.CAMERA"
        tools:ignore="PermissionImpliesUnsupportedChromeOsHardware" />

    <application
        android:name=".application.HealthyPetApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.HealthyPet"
        tools:targetApi="31">
        <activity
            android:name=".activity.ToCompleteActivity"
            android:exported="false" />
        <activity
            android:name=".activity.CropImageActivity"
            android:exported="false"
            android:theme="@style/Theme.Camera" />
        <activity
            android:name=".activity.CameraActivity"
            android:exported="false"
            android:theme="@style/Theme.Camera" />
        <activity
            android:name=".activity.RegisterPetActivity"
            android:exported="false" />
        <activity
            android:name=".activity.ListPetActivity"
            android:exported="false" />
        <activity
            android:name=".activity.WelcomeActivity"
            android:exported="false" />
        <activity
            android:name=".SplashActivity"
            android:exported="true"
            android:theme="@style/Theme.App.Starting">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>